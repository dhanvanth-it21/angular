import { Injectable } from '@angular/core';
import { BehaviorSubject, Observable } from 'rxjs';
import { of } from 'rxjs';
@Injectable({
  providedIn: 'root'
})

export class RxjsService {

  // text: string = "Dhanvanth";


  // // BehaviorSubject to hold and emit the current text value.
  // mySubject = new BehaviorSubject<string>(this.text);

  // // Observable created from the BehaviorSubject to allow subscription.
  // mySubjectOvervable = this.mySubject.asObservable();


  // updateText(data: string) {
  //   this.text = data;
  //   this.mySubject.next(this.text);
  // }

  



  

}
